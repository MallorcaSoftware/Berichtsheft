{% extends 'BerichtsheftBaseBundle::base.html.twig' %}
{% block title %}Ausbildungsnachweis Nr. {{ berichtsheft.number }} {{ parent() }}{% endblock %}
{% block content %}

    <div class="row top-margin-row">
        <div class="col-lg-12">
            <div class="actions">
                <div class="btn-group">
                    <a href="{{ path('berichtsheft_base_dashboard') }}" class="btn btn-default"><span class="glyphicon glyphicon-arrow-left"></span> zurück</a>
                    <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                        <span class="glyphicon glyphicon-export"></span> exportieren <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu" role="menu">
                        <li><a href="{{ path('berichtsheft_base_tbs') }}"><span class="glyphicon glyphicon-edit"></span> Word / OpenOffice</a></li>
                        <li><a href="{{ path('berichtsheft_base_pdf') }}"><span class="glyphicon glyphicon-file"></span> PDF</a></li>
                    </ul>
                </div>
            </div>
            {{ include('BerichtsheftBaseBundle:Berichtsheft:berichtsheft.html.twig', { 'berichtsheft': berichtsheft }) }}
        </div>
    </div>

{% endblock %}